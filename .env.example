# Create a Vercel token with access to this project. Never expose it to the browser.
VERCEL_ANALYTICS_TOKEN=

# Vercel supplies VERCEL_PROJECT_ID and VERCEL_ORG_ID to deployed projects when
# system environment variables are enabled. Set these locally if needed.
VERCEL_PROJECT_ID=
VERCEL_ORG_ID=
